'use strict';

var _tapeCatch = require('tape-catch');

var _tapeCatch2 = _interopRequireDefault(_tapeCatch);

var _keyIsDown = require('../src/keyIsDown');

var _keyIsDown2 = _interopRequireDefault(_keyIsDown);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

(0, _tapeCatch2.default)('keyIsDown', function (module) {
  (0, _tapeCatch2.default)('keyIsDown()', function (assert) {
    assert.ok(_keyIsDown2.default instanceof Function);
    assert.end();
  });

  (0, _tapeCatch2.default)('keyIsDown.all()', function (assert) {
    assert.ok(_keyIsDown2.default.all instanceof Function);
    assert.end();
  });

  (0, _tapeCatch2.default)('keyIsDown.any()', function (assert) {
    assert.ok(_keyIsDown2.default.any instanceof Function);
    assert.end();
  });

  (0, _tapeCatch2.default)('keyIsDown.list', function (assert) {
    assert.ok(_keyIsDown2.default.list instanceof Array);
    assert.end();
  });

  module.end();
});

